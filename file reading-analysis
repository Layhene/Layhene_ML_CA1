dfx = pd.read_csv("log2.csv")
print(df.head())
print(df.isnull().sum())
print(df['Action'].value_counts())
plt.figure(figsize=(8, 6))
sns.countplot(x='Action', data=df)
plt.title('Distribution of Action')
plt.xlabel('Action')
plt.ylabel('Count')
plt.show()
